// frontend/src/pages/admin/AdminLayout.js
import React from 'react';
import { Outlet } from 'react-router-dom';
import Sidebar from '../admin/components/AdminSidebar'; // Adjust the import path as necessary
import './Admin.css';
import bomareLogo from '../../assets/bomare_logo.png'; // Make sure bomare-logo.png is in ./assets/

const AdminLayout = () => {
  return (
    <div className="admin-layout">
      <Sidebar />
      <main className="admin-content">
        <header className="admin-header">
            {/* The page title will be rendered by each specific page component within the Outlet */}
            <img src={bomareLogo} alt="Bomare Company" className="admin-logo" />
        </header>
        <div className="admin-page-content">
          <Outlet /> {/* This is where the routed page component will render */}
        </div>
      </main>
    </div>
  );
};

export default AdminLayout;